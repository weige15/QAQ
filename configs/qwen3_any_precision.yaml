# Explicit QAQ mapping for the verified Qwen3-4B module tree.
# This file is passed to the pinned Any-Precision analyzer; it is not upstream support.
architecture: "Qwen3ForCausalLM"
arch_config:
  model_name: "model"
  layers_name: "layers"
  module_names:
    - "self_attn.q_proj"
    - "self_attn.k_proj"
    - "self_attn.v_proj"
    - "self_attn.o_proj"
    - "mlp.gate_proj"
    - "mlp.up_proj"
    - "mlp.down_proj"
